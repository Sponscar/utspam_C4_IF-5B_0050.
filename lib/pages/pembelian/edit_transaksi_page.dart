import 'package:flutter/material.dart';

class EditTransaksiPage extends StatelessWidget {
  const EditTransaksiPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Edit Transaksi")),
    );
  }
}
