import 'package:flutter/material.dart';

class FormPembelianPage extends StatelessWidget {
  const FormPembelianPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Form Pembelian")),
    );
  }
}
